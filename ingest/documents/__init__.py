"""The document pipeline: parse, hash, page-split, extract, cite.

`ingest/trackers/` reads what the fund *says* about its holdings. This package
reads what the *documents* say, which is the evidence the audit letter actually
asks for.
"""
